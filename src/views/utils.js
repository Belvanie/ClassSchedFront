
// Convertir la liste des cours reçus de l'API en la structure de données utilisée par l'app.
export function convertToTimetable(cours) {
    // Logique de mise en majuscule de la première lettre
    alert(cours)
}